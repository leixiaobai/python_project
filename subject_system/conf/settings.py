#!/usr/bin/env python
# -*- coding:utf-8 -*-
# Author: Xiaobai Lei
import os
top_path = os.path.dirname(os.path.dirname(__file__))    # 最顶层目录路径
username_path = os.path.join(top_path,"db" ,"username") # 用户信息路径
grade_obj = os.path.join(top_path,"db" ,"grade_obj") # 用户信息路径
school_obj = os.path.join(top_path,"db" ,"school_obj") # 用户信息路径
course_obj = os.path.join(top_path,"db" ,"course_obj") # 用户信息路径
teacher_obj = os.path.join(top_path,"db" ,"teacher_obj") # 用户信息路径
student_obj = os.path.join(top_path,"db" ,"student_obj") # 用户信息路径

